import React from "react"
import Layout from "../../../../components/layout"

const ClientOnlyPrioritizeStatic = props => (
  <Layout>
    <h1 data-testid="title">client-only/prioritize static</h1>
  </Layout>
)

export const Head = () => <title>Client-Only Prioritize static</title>

export default ClientOnlyPrioritizeStatic
