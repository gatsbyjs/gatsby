# Hello, world!

---

export const val = useInputByKey('myVal')
export const [myVal, ValInput] = useInput()
export const path = 'hello'

Write a hello file with "{val}".

<ValInput />

<File path={path} content={myVal} />

---

That's it, we did it! We wrote "{val}" to <code>{path}</code>!