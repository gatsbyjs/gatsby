const path = require(`path`);

module.exports = {
  plugins: [
    {
      // Resolving to absolute path for Yarn2/Pnpm compatibility reasons
      resolve: path.resolve(__dirname, `../gatsby-theme-test`)
    },
    `gatsby-theme-child`
  ]
}
