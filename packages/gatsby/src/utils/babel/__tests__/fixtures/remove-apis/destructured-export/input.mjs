export const {
  getServerData,
} = () => {
  return {
    props: {},
  }
}

export const {
  config,
  config2,
} = () => {
  return {
    defer: true,
  }
}

export const { anotherFunction } = () => `test`
