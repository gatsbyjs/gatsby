/**
 * A pretty cool jsdoc example
 * @param {string} apple A nice crispy apple
 * @example
 * const apple = require('apple')
 * apple()
 */
var apple = function apple(_apple) {
  console.log(`hi`);
};