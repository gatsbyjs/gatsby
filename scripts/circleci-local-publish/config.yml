storage: ./storage
auth:
  htpasswd:
    file: ./htpasswd
uplinks:
  npmjs:
    url: https://registry.npmjs.org/
packages:
  "**":
    access: $all
    proxy: npmjs
    publish: $all
logs:
  - { type: stdout, format: pretty, level: error }
listen:
  - localhost:4873
