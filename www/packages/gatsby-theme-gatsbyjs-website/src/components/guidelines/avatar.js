/** @jsx jsx */
import { jsx } from "theme-ui"

export default function Avatar() {
  return (
    <div
      sx={{
        bg: `grey.10`,
        borderRadius: 6,
        flex: `0 0 auto`,
        height: `avatar`,
        lineHeight: `solid`,
        width: `avatar`,
        mr: 2,
      }}
    />
  )
}
