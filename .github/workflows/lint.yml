name: Lint

on: pull_request

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: pieh/eslint-action@8dbc0eada646509aa2b7eeda3a510503be785c6f
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
