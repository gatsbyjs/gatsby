name: Lint

on: pull_request

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: pieh/eslint-action@c87cf81298b12134179d985b3e7d0b28d7ba6700
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
