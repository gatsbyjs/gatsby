import React from "react"

import Layout from "../components/layout"

const IndexPage = () => (
  <Layout>
    <h1>Hi people</h1>
    <p>
      This site is to demonstrate using multiple local plugins, you should see
      output in the terminal showing that the plugins are being registered!
    </p>
  </Layout>
)

export default IndexPage
