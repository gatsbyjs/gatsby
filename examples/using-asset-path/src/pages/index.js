import React from "react"

const Home = () => (
  <div>
    <h1>Using an asset path</h1>
    <p>
      View the source of this page to see where asset files are
      being served from based on your gatsby-config.
    </p>
  </div>
)

export default Home
